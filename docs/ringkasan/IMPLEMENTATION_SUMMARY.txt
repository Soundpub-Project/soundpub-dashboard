=== IMPLEMENTASI RELEASE WORKFLOW BARU ===

STATUS FLOW:
1. pending_paid -> processing (via Konfirmasi)
2. processing -> active (via Aktifkan dengan UPC/ISRC)
3. processing -> rejected (via Reject dengan reason)

FILES CREATED:
✓ supabase/migrations/20260725103400_add_processing_status_and_rejection_reason.sql
✓ src/components/releases/ActivateReleaseModal.tsx
✓ src/components/releases/RejectReleaseModal.tsx

FILES MODIFIED:
✓ src/integrations/supabase/types.ts (added rejection_reason field)
✓ src/pages/Releases.tsx (added modals, handlers, status badges)
✓ src/pages/ReleaseDetail.tsx (added rejection reason display)

CHANGES SUMMARY:

1. Database Schema:
   - Added 'processing' status to releases
   - Added 'rejection_reason' TEXT column to releases table
   
2. TypeScript Types:
   - Updated Release interface with rejection_reason field
   
3. New Components:
   - ActivateReleaseModal: Input UPC untuk release & ISRC untuk tracks
   - RejectReleaseModal: Input alasan penolakan
   
4. Releases Page Updates:
   - Added state for activate and reject dialogs
   - Added handlers: handleConfirmRelease, handleOpenActivateDialog, handleOpenRejectDialog
   - Updated dropdown menu for pending_paid status: "Konfirmasi" button
   - Added dropdown menu for processing status: "Aktifkan" and "Reject" buttons
   - Updated getStatusBadge() and getStatusLabel() to include 'processing'
   - Integrated new modals
   
5. ReleaseDetail Page Updates:
   - Added rejection_reason to Release interface
   - Added visual display for rejection reason (red alert box)
   - Updated getStatusBadge() to include processing status
   - Added AlertCircle icon import

NEXT STEPS FOR DEPLOYMENT:
1. Run migration: Execute the SQL file in Supabase Studio
2. Test the workflow:
   - Create a release and pay
   - Admin: Konfirmasi (pending_paid -> processing)
   - Admin: Aktifkan with UPC/ISRC (processing -> active)
   - OR Admin: Reject with reason (processing -> rejected)
3. Verify rejection reason displays correctly on detail page
